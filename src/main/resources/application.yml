spring:
#Configuración MySQL
  datasource:
    url: jdbc:mysql://localhost:3306/programaula?allowPublicKeyRetrieval=true&useSSL=false&useTimezone=true&serverTimezone=GMT&characterEncoding=UTF-8
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
#Configuración Creación de tablas (Hibernate)
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect 
        
  mail: 
    host: smtp.gmail.com
    port: 587
    username: programaula2021@gmail.com
    password: ProgramAula2021
    properties:
        mail:
            smtp:              
                auth: true
                starttls:
                   enable: true
                   
#Que thymeleaf no guarde datos en cache
  thymeleaf:
   cache: false
   
  mail:
    host: smtp.gmail.com
    port: 587
    username: mymail@gmail.com
    password: mypassword
    protocol: smtp
    tls: true
    properties.mail.smtp:
        auth: true
        starttls.enable: true
        ssl.trust: smtp.gmail.com

server:
  port: 8080
  

